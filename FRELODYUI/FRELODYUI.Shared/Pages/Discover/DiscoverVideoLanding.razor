@page "/play/{VideoId}"
@page "/discover/landing/{VideoId}"
@layout LandingLayout

<DiscoverVideoContent VideoId="@VideoId" BackUrl="/" />

@code {
    [Parameter] public string VideoId { get; set; } = default!;
}

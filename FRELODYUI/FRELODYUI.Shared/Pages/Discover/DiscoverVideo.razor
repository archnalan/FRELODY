@page "/discover/{VideoId}"
@rendermode InteractiveWebAssembly

<DiscoverVideoContent VideoId="@VideoId" BackUrl="/discover" />

@code {
    [Parameter] public string VideoId { get; set; } = default!;
}
